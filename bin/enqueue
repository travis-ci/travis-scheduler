#!/usr/bin/env ruby

$stdout.sync = true

$LOAD_PATH << 'lib'

require 'bundler/setup'
require 'travis/enqueue'


app = Travis::Enqueue::Schduler.new

Travis.logger.info('[enqueue] setting up schduler app')
app.setup

Travis.logger.info('[enqueue] start queuing dem jobs')
app.run
