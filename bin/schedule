#!/usr/bin/env ruby

$stdout.sync = true

$LOAD_PATH << 'lib'

require 'travis/scheduler'

app = Travis::Scheduler::Schedule.new

Travis.logger.info('[scheduler] setting up scheduler app')
app.setup

Travis.logger.info('[scheduler] start queuing dem jobs')
app.run
